import { Modal, ModalContent, ModalTrigger } from "@layer-ui/modal";

export default function ModalPage() {
  return (
    <Modal>
      <ModalTrigger>OPEN</ModalTrigger>
      <ModalContent>???</ModalContent>
    </Modal>
  );
}
